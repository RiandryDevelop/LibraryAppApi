class Book {
    constructor(id, title,  isbn, author) {
      (this.id = id),
        (this.title = title),
        (this. isbn =  isbn),
        (this.author = author)
    }
  }
  
  export default Book;